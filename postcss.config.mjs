const config = {
  plugins: {
    "@tailwindcss/postcss": {
      // theme: {
      //   animation: {
      //     bounce: "bounce 1s infinite",
      //     fadeIn: "fadeIn 1s ease-in-out",
      //   },
      // }
    }
  }
};

export default config;
